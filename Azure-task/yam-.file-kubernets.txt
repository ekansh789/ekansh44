apiVersion:    #bascially it show that which version is kubernetes API to create the object depends on what we are creating we have to use correct version
kind : type of object
metadata:   its is always in dictonray form like in key value pair it is used as a dataa for the object we are creating


